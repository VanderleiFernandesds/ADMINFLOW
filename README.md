# AdminFlow

Projeto Node.js com Express, MySQL e frontend estatico para gestao de usuarios.

## Como rodar

1. Configure o arquivo `.env`.
2. Instale as dependencias:

```bash
npm install
```

3. Inicie o servidor:

```bash
npm run dev
```

O frontend fica disponivel em `http://localhost:3000`.
